import { PagePlaceholder } from "@/components/ui/page-placeholder";

export default function LoginPage() {
  return <PagePlaceholder title="Connexion" description="Authentification Supabase (Phase 3)." />;
}

import { PagePlaceholder } from "@/components/ui/page-placeholder";

export default function ResidencesSearchPage() {
  return (
    <PagePlaceholder
      title="Résultats de recherche"
      description="Marketplace : filtres ville, dates, équipements, budget (Phase 5)."
    />
  );
}

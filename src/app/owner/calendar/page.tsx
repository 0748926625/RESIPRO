import { PagePlaceholder } from "@/components/ui/page-placeholder";

export default function OwnerCalendarPage() {
  return (
    <PagePlaceholder
      title="Calendrier"
      description="Vue jour / semaine / mois, réservations et blocages (Phase 6)."
    />
  );
}

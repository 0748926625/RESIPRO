import { PagePlaceholder } from "@/components/ui/page-placeholder";

export default function AdminSettingsPage() {
  return (
    <PagePlaceholder
      title="Paramètres plateforme"
      description="Nom, logo, couleurs, Mobile Money, commissions (Phase 2)."
    />
  );
}

import { PagePlaceholder } from "@/components/ui/page-placeholder";

export default function AdminPropertiesPage() {
  return (
    <PagePlaceholder
      title="Résidences"
      description="File de validation / suspension des résidences (Phase 5)."
    />
  );
}

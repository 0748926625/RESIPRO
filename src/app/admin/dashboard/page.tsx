import { PagePlaceholder } from "@/components/ui/page-placeholder";

export default function AdminDashboardPage() {
  return (
    <PagePlaceholder
      title="Tableau de bord Super Admin"
      description="Vue globale plateforme + alertes (Phase 13)."
    />
  );
}

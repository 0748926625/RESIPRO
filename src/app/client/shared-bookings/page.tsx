import { PagePlaceholder } from "@/components/ui/page-placeholder";

export default function ClientSharedBookingsPage() {
  return (
    <PagePlaceholder
      title="Réservations partagées"
      description="Rejoindre ou créer une demande de partage à deux (Phase 8)."
    />
  );
}
